---
/**
 * Login Page — #335 hero redesign
 * /login — public route (no auth guard)
 *
 * Uses AuthLayout in "hero" variant to bypass the default auth-card shell
 * and own the full split layout (LitCropWordmarkMorph on top/left,
 * LoginForm on the bottom/right).
 */
import AuthLayout from '../layouts/AuthLayout.astro';
import LoginForm from '../components/LoginForm';
import Toast from '../components/Toast';
import LitCropWordmarkMorph from '../components/brand/LitCropWordmarkMorph.astro';
---

<AuthLayout title="Log in" variant="hero">
  <div class="login-split">
    <div class="login-hero-wrap" aria-hidden="true">
      <LitCropWordmarkMorph variant="animated" />
    </div>
    <section class="login-form-panel">
      <div class="login-card">
        <h1 class="login-title">Welcome back</h1>
        <p class="login-sub">Sign in to manage your farm</p>
        <LoginForm client:load />
      </div>
    </section>
  </div>
  <Toast client:load />
</AuthLayout>

<style>
  /* ============================================================
     Login page layout — #335
     Mobile (default): stacked hero on top, form below
     Desktop (≥769px): split 60/40 hero left, form right
     ============================================================ */
  .login-split {
    display: grid;
    grid-template-columns: 1fr;
    min-height: 100vh;
    background: var(--color-background);
  }

  .login-hero-wrap {
    /* 1/1 aspect + 42vh cap. On tall mobile viewports the cap binds
       (430×391 on iPhone 16 Pro Max), giving ~19px symmetric clipping
       that keeps most of the line art visible while still fitting the
       form below without scroll. Desktop (≥769px) overrides below. */
    aspect-ratio: 1 / 1;
    max-height: 42vh;
    overflow: hidden;
  }

  .login-form-panel {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px 20px 24px;
    background: var(--color-background);
  }

  .login-card {
    width: 100%;
    max-width: 400px;
    background: var(--color-surface);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
    padding: 28px 24px 24px;
    border: var(--border-default);
  }

  /* Mobile default: hide title + subtitle so the hero animation takes the space. */
  .login-title,
  .login-sub {
    display: none;
  }

  .login-title {
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text);
    margin: 0 0 6px;
    letter-spacing: -0.01em;
  }

  .login-sub {
    font-size: var(--font-size-base);
    color: var(--color-gray-500);
    margin: 0 0 24px;
  }

  /* ---------- desktop split (≥769px) ---------- */
  @media (min-width: 769px) {
    .login-split {
      grid-template-columns: minmax(0, 6fr) minmax(0, 4fr);
      min-height: 100vh;
    }
    .login-hero-wrap {
      aspect-ratio: auto;
      max-height: none;
      height: 100vh;
      border-right: 1px solid var(--color-gray-300);
    }
    .login-form-panel {
      padding: 48px 40px;
    }
    .login-title,
    .login-sub {
      display: block;
    }
  }

  /* ---------- wider desktop (≥1200px) ---------- */
  @media (min-width: 1200px) {
    .login-card {
      max-width: 420px;
    }
  }
</style>
