# MVP-PLUS-SCENARIO.md — MVP+ Field Evaluation Scenario

> Date: 2026-03-22
> Status: DRAFT — for review and alignment
> Scope: MVP+ (v1.0) — 3 users, 2 farms (1 demo + 1 user-created)
> Context: Refines USE-CASES.md with concrete evaluation scenario

---

## 1. Personas

| ID | Name | Role | Description |
|----|------|------|-------------|
| **A** | Muk | Site Admin | Operates and supports the MVP+ LitCrop prototype. Creates demo data, onboards B and C, monitors system stats via admin-only views. |
| **B** | Kiku | Farm Manager | Creates and manages his own farm. Full read/write access. Sets up crops, installs camera nodes, invites observers. |
| **C** | Yama | Farm Observer | Read-only user. Cannot create farms. Joins B's farm via invite or apply-to-join. Views crops, watches time-lapse. |

### Role Summary

| Capability | A (Admin) | B (Manager) | C (Observer) |
|------------|:---------:|:-----------:|:------------:|
| Create farm | Yes (demo seed) | Yes | No |
| Edit farm / crops | Yes (demo) | Yes (own farm) | No |
| View farm data | All farms | Own farms | Joined farms |
| Install camera node | — | Yes | No |
| Invite / approve users | — | Yes (own farm) | No |
| Apply to join a farm | — | — | Yes |
| Access admin stats | Yes | No | No |
| Use AI assistant | Yes | Yes | Yes |

---

## 2. Scenario Overview

**Setup**: 3 end-users, 2 farms
- **Demo farm** — pre-seeded by A with sample data for onboarding
- **B's farm** — created by B through the farm creation wizard

---

## 3. Scenario Steps

### Step 1: Onboarding

| Actor | Action |
|-------|--------|
| **A** | Creates demo farm with pre-seeded data (beds, crops, sample images) so new users can explore without creating a farm first. |
| **A** | Onboards B and C to the LitCrop prototype (account creation via Cognito). |
| **A** | Monitors onboarding progress via admin-only stats view. |
| **B** | Logs in for the first time. Sees the Demo farm. Explores the app. Optionally uses AI assistant to learn how things work. |
| **C** | Logs in for the first time. Sees the Demo farm. Browses as a reader. Optionally uses AI assistant for guidance. |
| **B, C** | Both are now onboarded and familiar with the app through the Demo farm. |

### Step 2: New Farm Creation (B)

1. B optionally asks the AI assistant: *"How do I create a farm?"* — it answers with guidance.
2. B enters the **[Profile]** menu. It shows two sections: **"Farm"** and **"You"**.
3. The **"Farm"** section lists farms B belongs to — currently only **"Demo"**.
4. B taps the **[+]** button in the Farm section to start the farm creation wizard.
5. Wizard steps:
   - **Name** — B chooses a name for the farm.
   - **Description** — B writes a short description.
   - **Location** — B picks the location on an embedded map (F-09). The selected point auto-fills latitude and longitude fields as a preview.
6. B saves the farm. The wizard detects B is currently on the Demo farm and asks: *"Switch to the new farm?"*
7. B chooses **"Yes"**. The wizard closes. The entire LitCrop app now shows B's newly created farm.
8. The farm profile is visible in **[Profile]** as read-only, including a members list (currently only B).
9. B can see a **[Switch Farm]** button to toggle between "Demo" and the new farm. B stays on the new farm for now.

### Step 3: New Crops (B)

1. B enters the **[Crops]** menu. Two view modes are available: **"List"** and **"Layout"** (toggle buttons).
2. In **Layout** mode, B creates a bed layout for the farm:
   - Specifies **rows** and **columns** (MVP+ limit: 5 max each).
3. B taps a bed cell — a prompt asks: *"What crop do you plant here?"*
4. B selects a crop and saves. (MVP+ constraint: one crop per bed, crop is tied to the bed.)
5. Repeats for other beds as desired.

### Step 4: Camera Node Setup (B)

1. B has a physical camera (Raspberry Pi) to install at the farm. For MVP+: **one camera per bed**.
2. B enters the **[Manage]** page and sets up an IoT device:
   - Configures the device in the web app.
   - Attaches/associates it to one of the beds.
3. B physically installs and configures the camera node at the farm.
4. Images begin uploading. B sees the pictures appearing in the targeted crop's (bed's) data view, rendered in the app.

### Step 5: Sharing & Monitoring Together

1. **MVP+ approach**: A or B adds C as a member of B's farm (admin-managed — creates a `FARM_MEMBER` record). *(Self-service invite/apply workflow deferred to PROD-1.)*
2. C now has B's farm added to the Demo farm in the **[Profile] → Farm** section.
3. C switches to B's farm. Now **all three (A, B, C)** can view and monitor the farm.
4. C enters **[Crops]** menu, taps a crop/bed, and sees a **"Time-lapse"** option (available when the bed has enough images).
5. C taps it and enjoys the time-lapse replay — automatically generated by LitCrop from uploaded camera images.

---

## 4. Use Cases per Persona

| Persona | Use Cases |
|---------|-----------|
| **A (Muk)** | Seed demo farm, create user accounts, add members to farms, monitor admin stats, provide support |
| **B (Kiku)** | Explore demo farm, create own farm (wizard + map), set up bed grid, assign crops, configure camera locally, add C to farm, daily crop monitoring, use AI assistant |
| **C (Yama)** | Explore demo farm, switch to B's farm, browse crops/beds, watch time-lapse replay, check weather, use AI assistant |

---

## 5. Scope Decisions (confirmed 2026-03-22)

| # | Item | MVP+ Scope | Deferred To |
|---|------|-----------|-------------|
| 1 | Multi-farm foundation (N1) | **Yes** — schema, API, farm list, farm switcher | — |
| 2a | Membership: admin-managed | **Yes** — A/B adds C via FARM_MEMBER record | — |
| 2b | Membership: invite/apply workflow | — | PROD-1 |
| 3 | Bed-grid layout (rows × cols, 5 max) | **Yes** — replaces freeform plot wizard | — |
| 4a | IoT data pipeline (images → bed) | **Yes** — existing simulator + bed association | — |
| 4b | IoT device web config UI ([Manage] page) | — | PROD-1 |
| 5 | Demo farm seed data | **Yes** — pre-seeded by admin for onboarding | — |
| 6 | Role model (Admin / Manager / Observer) | **Yes** — via farm membership with role field | — |
| 7 | Profile page redesign (farm list + switch) | **Yes** — required for multi-farm UX | — |

---

## 6. Scope Impact Analysis (vs original MVP+ plan)

| Area | Original MVP+ Plan | Revised MVP+ | Change Type |
|------|-------------------|-------------|-------------|
| User roles | Single owner + Option D (relaxed GET) | 3 distinct roles (Admin / Manager / Observer) | **Expanded** |
| Farm count | 1 farm per user | Multiple farms per user (Demo + created) + farm switching | **Pulled forward from PROD-1** |
| Onboarding | Self-service register + farm setup | Demo farm pre-seeded + guided onboarding with AI | **New** |
| Crops/Layout | Freeform plot creation wizard | Bed grid layout (rows × cols, 5 max) + crop-per-bed model | **Restructured** |
| Camera/IoT | Camera simulator + phone upload | Same + local config (web UI deferred to PROD-1) | **Minimal change** |
| Sharing | Option D (shared URL read-only) | Admin-managed membership (invite/apply deferred to PROD-1) | **Stepped approach** |
| Profile page | Basic settings | Farm list + farm switching + member list | **New UI** |
| Estimated effort | ~12-14h | ~22-26h | **+10-12h** (revised after ultrathink review) |

---

> Updated 2026-03-22 | Formalized from rough sketch | Scope decisions confirmed | Integrated into USE-CASES.md §7
